# 📊 데이터베이스 설계 및 분석 학습 기록.

## 1. 개념적/논리적 DB 설계 및 ERD
* **개념적 설계:** 사용자 요구사항을 분석하여 핵심 객체(Entity)를 뽑아내고 관계를 정의하는 단계.
* **논리적 설계:** 개념적 모델을 실제 테이블 구조로 변환하고 정규화를 수행하는 단계.
* **ERD (Entity-Relationship Diagram):** 테이블 간의 관계를 시각적으로 나타낸 도표.
    * [cite_start]본 프로젝트에서는 `mermaid.js`를 사용하여 스키마를 문서화할 예정입니다[cite: 64].

## 2. DB 명세서 (Database Specification)
DB 명세서는 설계된 테이블의 세부 정보를 기록한 문서입니다.
* **포함 항목:** 테이블명, 컬럼명, 데이터 타입(INT, VARCHAR 등), 제약조건(PK, FK, Not Null).

## 3. 프로젝트 요구사항 분석 기반 DB 파악
[cite_start]현재 프로젝트 프레젠테이션 자료([cite: 1])를 바탕으로 필요한 데이터를 분류했습니다.

### [cite_start]주요 데이터 엔티티 [cite: 240-265]
1. [cite_start]**사용자 데이터:** 계정, 이메일, 역할(관리자/유저), 경고/밴 내역, 휴식 기록 [cite: 241-245].
2. [cite_start]**프로필 데이터:** 캐릭터 이름, 설명, 나이, 키, 몸무게 등 [cite: 111-116].
3. [cite_start]**경제/콘텐츠:** 재화 보유량, 아이템 내역, 공지사항, 이벤트 정보 [cite: 246-257].

### 평가 및 제언
* **필요한 점:** 마스토돈과 연동되므로 마스토돈 계정 ID가 기본키(PK) 혹은 고유키로 중요하게 관리되어야 함.
* [cite_start]**보완할 점:** '후순위'로 밀린 게임 데이터(스탯, 전투 기록)를 고려하여 확장 가능한 구조로 설계 필요 [cite: 258-262].

## 4. DB 종류 및 특징 (RDBMS vs NoSQL)
| 특징 | RDBMS (관계형) | NoSQL (비관계형) |
| :--- | :--- | :--- |
| **구조** | 고정된 스키마, 테이블 방식 | 유연한 구조 (JSON, Key-Value 등) |
| **장점** | 데이터 무결성 보장, SQL 활용 | 대용량 처리, 빠른 확장성 |
| **예시** | PostgreSQL, MySQL | MongoDB, Redis |

> [cite_start]**관심 있는 DB:** 본 프로젝트는 마스토돈과의 일관성을 위해 **PostgreSQL** 사용을 권장하고 있습니다[cite: 277].

## [cite_start]5. 현재 프로젝트의 DB 기술 스택 [cite: 277]
* **Main DB:** PostgreSQL (유저 정보, 게시글, 관계 저장).
* **Mastodon DB 구조:** 마스토돈은 PostgreSQL 외에도 빠른 처리를 위해 **Redis**를 캐시로 사용합니다.

## 6. 파이썬 DB 모듈
[cite_start]우리 프로젝트는 **Flask** 백엔드를 사용하므로 다음 모듈을 검토합니다[cite: 99].
* **psycopg2:** PostgreSQL과 파이썬을 연결하는 기본 라이브러리.
* **Flask-SQLAlchemy:** Flask에서 DB를 객체 지향적으로 다루게 해주는 ORM (권장).
